spring:
  application:
    name: consumer-user
server:
  port: 8200
eureka:
  instance:
    prefer-ip-address: true #注册到服务中心使用ip进行注册
  client:
    service.url:
      defaultZone: http://localhost:8761/eureka/  #我们自己的服务中心地址
